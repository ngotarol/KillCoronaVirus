@model IEnumerable<FCE_KillCoronaVirus.Models.TipoExaman>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomTipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomTipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodTipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodTipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodTipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
